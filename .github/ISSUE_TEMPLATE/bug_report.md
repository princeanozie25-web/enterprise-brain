---
name: Bug report
about: Something behaves against the documented contract
labels: bug
---

**⚠️ NEVER paste tokens, private keys, or `tokens.json`/`private_key.pem`
contents. Redact bearer strings from curls. ⚠️**

**What happened / what should have happened**

**Surface**: `/v1` | console | bridge | doctor | ledger | estate | packaging

**Reproduction** (curl or SDK snippet, redacted):

**Ledger excerpt** (the relevant `audit.jsonl` rows — reasons matter):

**Doctor output** (`service doctor --json`, or the container health log):

**Versions**: commit/tag, `docker --version` (if containerized), OS.
